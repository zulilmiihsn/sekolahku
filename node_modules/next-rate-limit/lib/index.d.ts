import { NextRequest } from 'next/server';

type Options = {
  uniqueTokenPerInterval?: number;
  interval?: number;
};
declare function rateLimit(options?: Options): {
  checkNext: (req: NextRequest, limit: number) => Headers;
};

export { rateLimit as default };
